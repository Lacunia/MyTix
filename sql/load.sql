-- Run this script from the repository root with the MySQL client:
-- mysql --local-infile=1 -u <user> -p <database> < sql/load.sql
-- SOURCE is a MySQL client command; it loads the complete assignment dataset.
SOURCE data/sample_data.sql;
